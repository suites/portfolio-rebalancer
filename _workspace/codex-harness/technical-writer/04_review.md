# Review

- 공개 문서의 `Steady Allocator`와 `steady-allocator` 사용처를 전체 검색했다.
- 모든 공개 프로젝트명 표기를 `Portfolio Rebalancer`로 통일했다.
- `steady` CLI 명령은 기존 사용 방법과의 호환성을 위해 유지했다.
- 이름 변경이 안전 규칙, 시스템 동작 또는 실거래 범위를 바꾸지 않는지 확인했다.
- 남은 이전 이름이 과거 결정 기록에만 존재하는지 최종 검색으로 검증한다.
