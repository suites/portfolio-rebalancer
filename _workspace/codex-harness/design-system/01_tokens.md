# Tokens

- `design/tokens.css`에 primitive와 semantic 토큰을 정의했다.
- 배경은 cool neutral, 기본 강조색은 독자적인 blue 계열을 사용한다.
- 상태는 normal, attention, blocked, pending, unknown으로 구분한다.
- 상태는 색상만 쓰지 않고 아이콘, 제목과 설명을 병기한다.
- 통화·비중 숫자에는 tabular numerals를 적용한다.
- focus ring, reduced motion과 forced colors 대응 토큰을 포함한다.
