# Accessibility

- `lang="ko"`, skip link, landmark와 heading 구조 적용
- 상태를 색상 외 아이콘, 이름과 설명으로 표시
- 네이티브 버튼, `aria-pressed`, `aria-live`와 명확한 focus ring 적용
- reduced motion과 forced colors 대응
- 390px에서 가로 overflow 없이 reflow 되는지 검증
- 후속 검증: axe, VoiceOver, 200% 확대와 320px reflow
